import React from 'react';
import './styles.css';

export default function SearchBar({value, handleSearchKey, clearSearch, formSubmit}) {
  return (
    <div >

        {/* <form onSubmit={formSubmit}>
            <input type="text" onChange={handleSearchKey} placeholder='search here' value={value} />
        </form>
        {value && <span onClick={clearSearch}>clear</span>}
        <button>Go</button> */}
        <br />
        <br />
        <br />
      
    </div>
  )
}
